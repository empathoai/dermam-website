# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

### Added
- Created `DERMA_M_TYPOGRAPHY_AND_ICON_AUDIT.md` at project root — documents all arbitrary heading classes, decorative icon usage per component, keep/remove classification for every icon import, and priority action list against DESIGN.md roles.
- Created `src/lib/typography.ts` — canonical Tailwind class string constants for every DESIGN.md typography role (`displayHeroClass`, `headingSectionClass`, `headingSectionSoftClass`, `headingSubsectionClass`, `cardTitleClass`, `bodyLgClass`, `bodyMdClass`, `bodySmClass`, `eyebrowClass`, `statNumberClass`, `statLabelClass`). Single source of truth; call sites add color token separately.
- Created `src/components/sections/shared/PostHeroOrientationSection.tsx` — canonical post-hero orientation primitive used by all hub and Tier 1 landing pages. Exports `OrientationCard` and `PostHeroOrientationSectionProps` types. Single typography system: eyebrow `text-[0.72rem]/tracking-[0.18em]`, H2 `clamp(2rem,3.8vw,3.35rem)/leading-[0.98]/tracking-[-0.04em]`, description `clamp(1rem,1.4vw,1.2rem)/leading-[1.65]`, card titles `text-[1.05rem] lg:text-[1.15rem]`, labels as plain `text-sm font-medium` with ` · ` separator. 2-card layouts auto-center with `max-w-4xl`. Optional per-card CTA link and section-level CTA.

### Changed
- Removed stats (`10+`, `1,000+`, `100%`) from `DifferentiatorsSection` entirely — no strip, no panel placement, no hidden markup. Section now contains only the left editorial panel and right proof blocks.
- Fixed `DifferentiatorsSection` stat overflow: removed large stats from inside the left panel (was causing `10+ 1,000+ 100%` collision at `statNumberClass` scale). Stats moved to a slim transparent strip below the two-column block — `text-[clamp(2rem,4vw,3.25rem)]` numbers in `text-base-100`, `statLabelClass` labels in `text-base-400`, thin `bg-white/20` vertical dividers, centered `max-w-lg lg:max-w-xl`. Left panel now contains only eyebrow → H2 → divider → body → mini-list → CTA link. Proof blocks changed from `bg-white/92` to `bg-base-100/92` for slightly reduced brightness over the image.
- Rebuilt `DifferentiatorsSection` (home): removed all decorative Lucide icon imports (`Award`, `Users`, `ShieldCheck`, `Microscope`, `BadgeCheck`, `BarChart`). Stats are now typography-led (`statNumberClass` + `statLabelClass`, no circular icon badge). Left panel upgraded to `bg-base-900/80 backdrop-blur-sm border border-white/10 rounded-[1.5rem]` with eyebrow + divider. Benefit proof cards use `01`/`02`/`03` editorial index with thin vertical divider, no icon circles.
- `PostHeroOrientationSection` H2 now uses `headingSectionClass` from `src/lib/typography.ts` (`clamp(1.9rem,3.6vw,3rem) font-bold leading-[1.05] tracking-[-0.035em] uppercase`) — aligned to DESIGN.md `heading-section` role. Previous value `clamp(2rem,3.8vw,3.35rem)` was not a documented role and made Tier 1 headings appear larger than hub headings.
- Normalized post-hero orientation sections across all hub and Tier 1 landing pages to `PostHeroOrientationSection`. Removed `EditorialDecisionCards.tsx` (superseded). `HubIntroSection` is now a thin adapter mapping `HubPageContent['intro'].decisionCards` to the canonical props. `PrfAudienceSection`, `DeepFacialAudienceSection`, `PostOpSurgeryTypesSection` now use the same component directly. Typography scale, card height, spacing, and label treatment are now identical across all 7 affected routes.: centered header (eyebrow/H2/body), then a 3-column card grid (2-column centered for dental). Each card has a muted `01`/`02`/`03` index, thin divider, title, body, small label pills, and a `Ver protocolos →` anchor link. Mobile stacks to single column. Added `decisionCards` field to `HubPageContent['intro']` type and populated data in `facialHubContent.ts`, `bodyHubContent.ts`, `laserLightHubContent.ts`, and `dentalHubContent.ts`.
- Rebuilt `HubIntroSection` as an editorial two-column layout: left column holds eyebrow/headline/body copy; right column renders objective-selector pills with `01`–`0N` numeric indices, `min-h-[44px]` touch targets, `rounded-[999px]` border treatment, and no color accents. Mobile stacks vertically with left-aligned text. Removed centered layout and SaaS chip styling.

### Fixed
- `GoogleReviews` dark variant was not rendering on hub pages: added `variant` prop to `GoogleReviews` component with full dark styling (`bg-base-900`, `text-base-100`/`text-base-300`, `border-white/10` cards, dark CTA), updated `ReviewText` to accept `isDark` for correct toggle-button colors, and passed `variant="dark"` from `HubPageTemplate`.

### Added
- Created [DERMA_M_SITE_ARCHITECTURE_AUDIT_INPUT.md](file:///d:/Derma_Content/DermaM-WebSite/DERMA_M_SITE_ARCHITECTURE_AUDIT_INPUT.md) containing a complete, structured extraction of the current website architecture, pages, sections, components, styling system, and content model in Spanish.
- Created `changelog.md` to track all project modifications.
- Added `catalogSections` data to facial and corporate hub content files (`src/data/facialHubContent.ts` and `src/data/bodyHubContent.ts`).
- Added type definition for `HubCatalogSection` in `src/types/hub.ts` and added optional `catalogSections` field to `HubPageContent`.
- Created `HubCatalogSections.tsx` component ([src/components/sections/hubs/HubCatalogSections.tsx](file:///d:/Derma_Content/DermaM-WebSite/src/components/sections/hubs/HubCatalogSections.tsx)) to render alternating editorial split blocks with white content panels, custom metadata, and custom CTAs.
- Added `catalogSections` data to laser/light and dental hub content files (`src/data/laserLightHubContent.ts` and `src/data/dentalHubContent.ts`).
- Added image and benefit icon mappings for Laser/Light and Dental hub sections in `src/components/sections/hubs/HubCatalogSections.tsx`.

### Modified
- Standardized all primary page heroes (Home, hubs, Tier 1 landings, Tier 3 treatments, IV Therapy, About, Booking, and Contact) to the fullscreen responsive hero contract using shared `hero-viewport` / `hero-content-viewport` classes, `100svh` minus header CSS variables, fixed-header page offset via `var(--header-height)`, and a 72px mobile navbar height. Updated `DESIGN.md` to document the new hero standard.
- Registered `changelog.md` in `AGENTS.md` as a mandatory registry for future edits.
- Replaced `HubIntroSection.tsx` with a compact objective navigation strip on the off-white canvas, rendering eyebrow, headline, description, and clickable chips/pills linking to the corresponding catalog section anchors.
- Added `chips` optional property in `src/types/hub.ts` and updated metadata files `src/data/facialHubContent.ts` and `src/data/bodyHubContent.ts` with the new navigation copy and chip target ids.
- Added `id={section.id}` and `scroll-mt-24` to the `<section>` elements in `HubCatalogSections.tsx` to enable smooth anchor jumps to each catalog section.
- Updated `TreatmentCardRail.tsx` ([src/components/ui/TreatmentCardRail.tsx](file:///d:/Derma_Content/DermaM-WebSite/src/components/ui/TreatmentCardRail.tsx)) to dynamically set the desktop columns count to `Math.min(items.length, 4)`, ensuring that groups of cards with fewer than 4 items (like the 2 featured cards in the Corporate Hub) are horizontally centered on desktop.
- Updated featured treatments selection/order on both facial and corporate hubs.
- Refactored `HubCatalogSections.tsx` component ([src/components/sections/hubs/HubCatalogSections.tsx](file:///d:/Derma_Content/DermaM-WebSite/src/components/sections/hubs/HubCatalogSections.tsx)) to render each service module as a large, horizontal editorial card. Normalized all desktop modules to a consistent fixed height of `420px` with uniform image/content/benefits dimensions.
- Enforced a visual Z-pattern on desktop (odd modules: image left, even modules: image right) while keeping identical visual sizing.
- Placed circular icons in Column 1 and benefits checklist in Column 2 (limited to a maximum of 3 items using slice). Applied line-clamping (`line-clamp-3` for description, `line-clamp-2` for bullet items and icon captions) to prevent vertical overflows, and changed alignment to `justify-start` (rather than `justify-between`) inside the left content column to prevent icons from drifting to the absolute bottom edge or colliding with the CTA divider row.
- Vertically aligned all CTA buttons to the exact bottom of the card using flex layout (`flex-col justify-between h-full`).
- Stacks all elements vertically on mobile (`[Image] [Eyebrow] [Treatment name] [Description] [Compact benefits] [Benefits list] [CTAs]`) with no horizontal scroll or image-right order.
- Confirmed that no routes, SEO, sitemap, public assets, or Tier 3 pages were modified.
- Migrated `/es/laser-y-luz` and `/es/dental` hubs to the updated catalog-sections Z-pattern layout structure, bypassing the old dense grid and adding objective navigation chips.
- Centered featured treatments on desktop for Laser/Light and Dental hubs, preserving mobile scroll behavior.
- Aligned `/es/laser-y-luz` content with the official catalog, displaying only three services: `Depilación Láser Diodo`, `IPL – Luz Pulsada Intensa`, and `Láser CO₂ Fraccionado`. Removed unconfirmed/outdated services from display.
- Aligned `/es/dental` content with the official catalog, displaying only two services: `Blanqueamiento Dental Estético` and `Limpieza Dental Profesional`. Removed unconfirmed/outdated services from display.
- Updated both `laserLightHubContent.ts` and `dentalHubContent.ts` data files (`catalogSections`, `featured`, `allGrid`, and `socialProof` lists) to strictly match official catalog services.
- Updated `/es/iv-therapy` intro section to align with the objective navigation strip of the hub system, adding objective chips linking to kit anchors.
- Replaced the IV Kit grid section on `/es/iv-therapy` with detailed Z-pattern service modules (`IVKitsCatalogSection`) matching the hub layouts.
- Updated `ivKits.ts` to display exactly the 11 official IV kits, removing all pricing, e-commerce checkout links, and unconfirmed kits.
- Updated types in `src/types/ivTherapy.ts` and configurations in `src/data/ivTherapyContent.ts` to support the new layout structure.
- Replaced the heavy post-hero check card sections with compact orientation strips on PRP (`PrfAudienceSection`), Limpieza Facial (`DeepFacialAudienceSection`), and Postoperatorio (`PostOpSurgeryTypesSection`) landing pages to match the hub intro styles.
- Replaced `PrfEducationSection` dark PRP vs PRF comparison table with a premium editorial split section: large rounded image (left column) + eyebrow / headline / body / subheading / three numbered editorial cards (right column). Section uses `bg-white`, DESIGN.md tokens, Poppins hierarchy, `border-[rgba(20,19,19,0.12)]`, `bg-canvas` cards, zero dark blocks, zero colorful icons. Layout stacks image-first on mobile, side-by-side on desktop (lg:grid-cols-2).
- Updated `src/data/prfLanding.ts` education block: removed `comparison` array and `ComparisonItem` usage; added `eyebrow`, `differentiatorHeading`, and `differentiators` array with three editorial copy objects (Fibrina como matriz natural / Estimulación gradual / Protocolo personalizado). Softened guarantee language throughout (apoya / ayuda a / puede contribuir / progresivo). Also softened `benefits[2].description` and relevant FAQ answers. File saved as UTF-8.
- Full PRF page text rewrite (text-only, no layout/structure/style changes). Updated `src/data/prfLanding.ts` with new premium editorial copy across all sections: hero subheadline and CTA, audience checks, education copy and differentiator cards, benefits titles and descriptions, protocol steps and info line, results headline, 7 FAQ items replaced, founder title/quote/cta, finalCTA headline/body/buttons, relatedTreatments card 3 title and description. Removed all absolute claims; applied soft language (puede ayudar / apoyar / progresivo / según cada piel / diagnóstico previo). Updated hardcoded texts in 6 components: `PrfAudienceSection.tsx` (eyebrow, headline, support text, chips), `PrfBenefitsSection.tsx` (H2), `PrfTestimonialsSection.tsx` (SectionHeader headline), `PrfRelatedTreatments.tsx` (H2), `PrfFAQSection.tsx` (H2), `PrfFinalCTA.tsx` (H2 inline markup, microcopy pill). TypeScript: zero errors.
- Refined proportions of `PrfEducationSection` (proportions-only, zero copy or structure change). Desktop: asymmetric two-column grid via `gridTemplateColumns: 'minmax(0,0.9fr) minmax(0,1.1fr)'` (46/54 split) with `items-center`; image capped at `maxHeight: 520px` with `aspectRatio: 4/5`; H2 scale reduced from `clamp(1.9rem,3.6vw,3rem)` to `clamp(1.6rem,2.8vw,2.25rem)` with `max-w-[580px]`; body copy reduced to `text-base`; right column gap reduced from `gap-8` to `gap-5`; card padding reduced from `px-6 py-5` to `px-4 py-3.5`; card gap reduced from `gap-4` to `gap-2.5`; card radius tightened from `rounded-[1rem]` to `rounded-[0.875rem]`. Mobile: separate stacked layout (`lg:hidden`) with landscape `aspectRatio: 4/3` image, H2 `clamp(1.45rem,5.5vw,1.9rem)`, body `text-sm`, same compact cards. Section vertical padding reduced from `py-24` to `py-20`. No comparison table restored. No other sections touched. TypeScript: zero errors.
- Converted `PostOpUrgencySection` on `/es/postoperatorio-west-palm-beach` from fear-based 3-card warning block into a premium two-column image-led editorial section. Files changed: `src/components/sections/postop/PostOpUrgencySection.tsx` (full rewrite) + `src/data/postOpLanding.ts` (replaced `urgency.blocks` with `urgency.intro` + `urgency.cards[3]`). Desktop layout: `minmax(0,0.9fr) minmax(0,1.1fr)` asymmetric grid, `items-center`, image left `aspectRatio: 5/4` `maxHeight: 520px`, `rounded-[1.25rem]`, `object-cover`, decorative offset border; right column: eyebrow / H2 `clamp(1.6rem,2.8vw,2.25rem)` / intro paragraph / border divider / subheading / 3 compact numbered editorial cards (`bg-white`, `px-4 py-3.5`, `gap-2.5`, `rounded-[0.875rem]`). Mobile: `lg:hidden` stacked block, `aspectRatio: 4/3` image, smaller H2, cards stacked. Image used: `urgency.image.src` = `/assets/treatments/postop-urgency.jpg` (existing path from data file). Removed: `AlertCircle`, `Check`, `Clock` icon imports, `warning`/`success`/`info` type system, all fear-based language ("sin drenaje especializado → fibrosis", "críticos"). Applied soft copy (ayuda a movilizar / puede ayudar a reducir / acompañamos). `PostOpSurgeryTypesSection` (orientation strip) unmodified. No other postop sections touched. TypeScript: zero errors.
- Redesigned `Tier3QuickFitSection` (`src/components/sections/tier3/Tier3QuickFitSection.tsx`) to match the premium Stitch layout design: horizontal metrics strip (Duración / Recuperación / Dolor) with thin vertical dividers and labels on top, followed by a horizontal divider line and a centered white card containing candidate/indication pills (`bg-canvas` off-white, rounded corners, circular check outline) adapting dynamically to the number of items. This updates all Tier 3 treatment pages (such as Microneedling, Hidrofacial, Peel Coreano, etc.) simultaneously.
- [Design Reference] Generated Stitch design proposal for `HubCatalogSections` component (tratamientos-faciales hub page). Prompt sent to Stitch project `10286135859825933988` (DERMA.M Skin Health design system, Poppins + Hamilton + monochromatic palette). Proposal covers: 3 editorial Z-pattern catalog cards (01 Preparación de piel, 02 Renovación superficial, 03 Bioestimulación), large watermark editorial number per card, pill-shaped treatment chips (`bg-canvas`, `text-secondary`), `Ideal para` checklist with circular check icons, refined CTA row (solid primary + outline secondary), and card backgrounds in `bg-white` over `bg-canvas` page surface. Screen ID: `5801a0ed0d5f4b2ea643a829d39b80de`.
- Implemented the Stitch-inspired `HubCatalogSections` treatment catalog layout in `src/components/sections/hubs/HubCatalogSections.tsx`: large editorial Z-pattern cards, oversized monochrome watermark numbers, image overlay number pills, treatment chips on `bg-canvas`, `Ideal para` checklist, and primary-solid / secondary-outline CTA hierarchy. Removed the previous compact benefit-icon row from the catalog cards while preserving existing hub data, anchors, routes, WhatsApp behavior, and section images.
- Updated `DESIGN.md` and `AGENTS.md` to document the validated HubCatalogSections editorial catalog pattern, sync implemented hero-height guidance with current code, clarify hub page ordering, and add an explicit DESIGN contract-sync guardrail for future UI changes.
- Refined `HubCatalogSections` CTA/checklist proportions after browser screenshot QA: checklist now occupies a full-width row above the CTA pair, preventing desktop checklist text from crowding or colliding with buttons while preserving the Stitch-inspired visual hierarchy.
- Fixed broken Laser/Light and Dental `HubCatalogSections` images by remapping catalog module image sources to existing treatment assets (`/assets/treatments/laser-luz/*` and `/assets/treatments/dental/*`), restoring the image-led editorial card treatment instead of gray image fallbacks.
- Fixed remaining hub data image paths for Faciales, Corporales, Láser & Luz, and Dental by replacing missing `/assets/images/*` references with existing treatment-family assets. Verified all four hub routes have no `/assets/` 404 responses in mobile browser QA and no horizontal overflow at 390px.
- Remapped safe non-clinical missing image references across About, Booking, Contact, Home, IV Therapy, Postoperatorio, Tier 3 treatments, and Nancy founder data to existing in-repository assets. Left clinical before/after result images and missing specialist portraits unresolved intentionally because they require real approved assets rather than generic substitutions.
- Replaced thick `border-l-4` founder quote side-tabs across Home, About, PRF, Limpieza Facial, and Postoperatorio founder sections with subtle `border-y border-border-subtle` editorial pull-quote treatments; documented the founder quote rule in `DESIGN.md`.
- Added `variant="dark"` prop to `GoogleReviews` component (`src/components/GoogleReviews.tsx`). Dark variant uses `bg-base-900` section background, `text-base-100/300/400` hierarchy, `bg-white/[0.05]` cards with `border-white/10`, yellow stars only, uppercase editorial tracking on eyebrow/author/CTA link, and WCAG AA accessible `aria-label` on rating and external link. Light variant behavior unchanged. Activated `variant="dark"` in `HubPageTemplate.tsx` for all four hub pages (Facial, Corporal, Laser/Luz, Dental) following the approved hub section order: Hero → Intro → Featured → Catalog → Dark Reviews → FAQ → FinalCTA.
- Added `CLAUDE.md` at repository root with dev commands, full architecture overview, design system token summary, critical constraints, and a step-by-step guide for adding Tier 3 treatment pages. Committed all accumulated session changes (36 files: components, sections, data, styles, docs) in a single `chore: session UI/UX polish, data updates, and add CLAUDE.md` commit (`cfb1920`) and pushed to `origin/main`.




